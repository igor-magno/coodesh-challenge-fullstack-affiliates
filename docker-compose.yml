version: '3'

services:

  api:
    build:
      context: ./api
    ports:
      - "3000:3000"
    volumes:
      - ./api:/usr/src/app

  web:
    build:
      context: ./web
    ports:
      - "80:3000"
    volumes:
      - ./web:/usr/src/app
